langcode: en
status: true
dependencies:
  config:
    - key.key.plausible_domain
    - plausible.settings
id: plausible_domain
label: 'Plausible domain'
config_type: system.simple
config_prefix: ''
config_name: plausible.settings
config_item: script.domain
key_id: plausible_domain
